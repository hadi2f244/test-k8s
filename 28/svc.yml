apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  labels:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/part-of: nextcloud
    app.kubernetes.io/type: cloud-system
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/part-of: nextcloud
    app.kubernetes.io/type: cloud-system
  ports:
    - port: 80
      targetPort: 80

